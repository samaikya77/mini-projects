var array = ['you', 'will', 'learn', 'javascript'];
var hasValue = array.includes('javascript');
if(hasValue) {
    console.log('Array contains a value.');
} else {
    console.log('Array does not contain a value.');
}